apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: second-thought-postgres-env
  namespace: second-thought-backend
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: second-thought-postgres-env
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: apps/second-thought-backend/postgres/env
